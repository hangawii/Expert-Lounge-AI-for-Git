import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ynconsulting.expertlounge',
  appName: 'Expert Lounge AI',
  webDir: 'dist'
};

export default config;

import React from 'react';

// FIX: Legacy Layout component stub to prevent build errors.
export const Layout = ({ children }: { children?: React.ReactNode }) => {
  return <>{children}</>;
};

export default Layout;
